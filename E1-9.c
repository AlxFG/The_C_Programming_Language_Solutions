#include <stdio.h>
int main(){
    int c,lc;
    while ((c=getchar())!=EOF){
        if (lc==' '&&c==' ');
        else if (c=='\t')
            putchar(' ');
        else
            putchar(c);
        lc=c;}
    return 0;
}

